#!/usr/bin/env zsh

# Fix for `zsh: corrupt history file /home/george/.zsh_history`
# https://shapeshed.com/zsh-corrupt-history-file/

cd $HOME
mv .zsh_history .zsh_history_bad
strings .zsh_history_bad > .zsh_history
fc -R .zsh_history
rm .zsh_history_bad
